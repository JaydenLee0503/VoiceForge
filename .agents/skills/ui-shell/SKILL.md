---
name: ui-shell
description: Build and refine premium dark-theme VoiceForge frontend pages and reusable UI components. Use when creating or polishing landing, dashboard, scenario, session, results, history, or shared shell UI for VoiceForge.
---

When using this skill:
- preserve the VoiceForge premium mission-control aesthetic
- prefer reusable components over page-specific duplication
- keep animations subtle
- use mock data first
- avoid overbuilding backend logic
- ensure all UI supports the core speaking-coach product direction

Workflow:
1. Inspect the existing feature, shared UI, and shell structure first.
2. Reuse or extract shared primitives before adding page-specific markup.
3. Keep pages implementation-ready with mock data and clean states.
4. Preserve visual calm: strong hierarchy, restrained glow, minimal motion.
5. Validate responsive behavior and keep dark mode aligned with VoiceForge.
